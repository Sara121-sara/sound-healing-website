[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NEXT_PRIVATE_TARGET = "server"

# 客户端路由重定向
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200